extends ../templates/default

block main
  section.background
    include ../components/breadcrumbs/breadcrumbs

    +profile-page({})
      +profile-center({
        title: 'Адреса доставки',
      })
        include ../react/providers/pages/lk-addresses/AddressProvider/AddressProvider