extends ../templates/default

block main
  section.background
    include ../components/breadcrumbs/breadcrumbs

    +profile-page({})
      +profile-center({
        title: 'Вы смотрели',
        sort: true,
        list: true,
        pagination: true,
      })